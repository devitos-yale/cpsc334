void setup() {
  //size(8160, 768); //ultimate desktop size
  //size(4608, 1360); //projector visual
  size(768, 227); //projector/6
}

void draw() {
  if (mousePressed) {
    fill(0);
  } else {
    fill(255);
  }
  ellipse(mouseX, mouseY, 80, 80);
}
